# Fetch and notify Crestron hardware/software updates

Usage: Create a `.env` file and populate it with your Slack Webhook path `SLACK_WEBHOOK_PATH=/services/etc/etc`.

To run: `node app.js` or `npm run start`